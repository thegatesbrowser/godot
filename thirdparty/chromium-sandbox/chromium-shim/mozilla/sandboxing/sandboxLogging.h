// the_gates: Firefox external code includes this header at
// mozilla/sandboxing/sandboxLogging.h. The real declarations live at
// chromium-shim/sandbox/win/sandboxLogging.h (because that's where the
// implementation .cpp sits). Forward to it.

#include "sandbox/win/sandboxLogging.h"
